# Job Board Frontend (Vite + React + TS)

## Setup

```bash
cd frontend-vite
npm install
echo "VITE_API_BASE=https://globaladvancedopc-assesment-2.onrender.com/api" > .env
npm run dev
```

Make sure the backend is running on `http://localhost:5000`.
